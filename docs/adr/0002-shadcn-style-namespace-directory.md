---
status: superseded by ADR-0003
---

# Shadcn-Style Namespace Directory

This decision was superseded when the MVP goal was clarified as item discovery rather than registry discovery only. The accepted direction still keeps `directory.json` as curated registry metadata, but the public product must also show Registry Items resolved from those registries.
